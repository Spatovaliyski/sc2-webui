{
  "name": "starcraft",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^4.13.0",
    "nodemon": "^2.0.2"
  },

  "scripts": {
    "build-css": "node-sass --include-path scss private/scss/master.scss public/css/master.css",
    "watch-css": "nodemon -e scss -x \"npm run build-css\""
  }
}
